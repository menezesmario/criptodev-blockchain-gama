const { assert, expect } = require('chai')
const request = require("request");


describe("Color Converter server", () => {

    describe("RGB to Hex", () => {

      it("status 200", () => {});

      it("color in hex", () => {});
    });
  
    describe("Hex to RGB", () => {

      it("status 200", () => {});

      it("color in RGB", () => {});
    });
  
  });


